import React from "react";
import { useState } from "react";

const Card = ({ item }) => {
    const [isLoading, setIsLoading] = useState(true);

    return (
        <div>
            Card
        </div>
    )
}

export default Card;